import { PageHero } from '../components/common/PageHero'
import { SectionHeading } from '../components/common/SectionHeading'
import { SEO } from '../components/common/SEO'
import { Button } from '../components/common/Button'
import { AustraliaRouteGraphic } from '../components/coverage/AustraliaRouteGraphic'
import { areas } from '../content/serviceAreas'
import { seo } from '../content/seo'
export function ServiceAreasPage() { return <><SEO {...seo.areas}/><PageHero eyebrow="Service areas" title="Local Knowledge. Broader Reach." copy="Transport enquiries across Sydney, regional NSW, ACT and interstate routes."/><section className="section bg-navy text-white"><div className="container-page grid items-center gap-10 lg:grid-cols-2"><div><SectionHeading eyebrow="Route enquiries" title="A clear view of the conversation" inverted copy="This is a stylised service-area graphic, not live fleet tracking. Routes and availability are confirmed based on individual requirements."/><Button to="/quote" variant="dark" className="mt-7">Ask About Your Area</Button></div><AustraliaRouteGraphic/></div></section><section className="section"><div className="container-page grid gap-10 md:grid-cols-2">{Object.entries(areas).map(([group, names])=><article key={group} className="border-t-2 border-gold pt-5"><h2 className="text-3xl font-bold">{group}</h2><div className="mt-5 flex flex-wrap gap-2">{names.map(name=><span className="border border-charcoal/15 px-3 py-2 text-sm" key={name}>{name}</span>)}</div></article>)}</div><p className="container-page mt-12 text-sm leading-6 text-slate">Service areas and interstate transport enquiries are considered subject to the freight, route, requested date and operational availability. Fixed daily routes are not implied.</p></section></> }
