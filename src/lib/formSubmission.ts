import type { BookingData, ContactData, QuoteData } from './formSchemas'
export type FormSubmissionResult = { success: true; reference?: string } | { success: false; message: string }
async function submit(endpoint: string | undefined, payload: unknown, kind: string): Promise<FormSubmissionResult> { if (!endpoint) { if (import.meta.env.DEV) { console.info(`[simulated development ${kind} submission]`, payload); return { success: true, reference: 'DEV-SIMULATED' } } return { success: false, message: 'Online form delivery is not configured. Please call or email us directly.' } } try { const response = await fetch(endpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) }); if (!response.ok) return { success: false, message: 'We could not send your request. Please try again or contact us directly.' }; return { success: true } } catch { return { success: false, message: 'We could not connect to the form service. Please try again or contact us directly.' } } }
export const submitQuoteForm = (data: QuoteData) => submit(import.meta.env.VITE_QUOTE_FORM_ENDPOINT, data, 'quote')
export const submitBookingForm = (data: BookingData) => submit(import.meta.env.VITE_BOOKING_FORM_ENDPOINT, data, 'booking')
export const submitContactForm = (data: ContactData) => submit(import.meta.env.VITE_CONTACT_FORM_ENDPOINT, data, 'contact')
