import type { InputHTMLAttributes, TextareaHTMLAttributes } from 'react'
type Props = InputHTMLAttributes<HTMLInputElement> & { label: string; error?: string; textarea?: false }
type TextareaProps = TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string; error?: string; textarea: true }
export function FormField(props: Props | TextareaProps) { const { label, error, textarea, id, ...field } = props; const fieldId = id ?? String(field.name); return <div><label className="mb-2 block text-sm font-bold text-charcoal" htmlFor={fieldId}>{label}</label>{textarea ? <textarea id={fieldId} className="min-h-28 w-full rounded-sm border border-charcoal/25 bg-white px-3 py-3 text-sm" aria-invalid={Boolean(error)} aria-describedby={error ? `${fieldId}-error` : undefined} {...field as TextareaHTMLAttributes<HTMLTextAreaElement>} /> : <input id={fieldId} className="min-h-12 w-full rounded-sm border border-charcoal/25 bg-white px-3 text-sm" aria-invalid={Boolean(error)} aria-describedby={error ? `${fieldId}-error` : undefined} {...field as InputHTMLAttributes<HTMLInputElement>} />}{error && <p id={`${fieldId}-error`} className="mt-1 text-sm font-semibold text-red" role="alert">{error}</p>}</div> }
