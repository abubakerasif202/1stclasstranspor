import { ArrowRight, CheckCircle2 } from 'lucide-react'
import { Link } from 'react-router-dom'
import { company } from '../../content/company'
import { Button } from '../common/Button'
import { RouteLine } from '../motion/RouteLine'
export function HomeHero() { return <section className="relative isolate overflow-hidden bg-ink pb-16 pt-16 text-white sm:pb-24 sm:pt-24"><div aria-hidden="true" className="truck-fallback absolute inset-y-0 right-0 w-full opacity-70 lg:w-[58%]"/><div aria-hidden="true" className="absolute inset-y-0 left-0 w-full bg-gradient-to-r from-ink via-ink/95 to-transparent lg:w-[72%]"/><div className="container-page relative"><div className="max-w-3xl"><p className="eyebrow">Australian Transport &amp; Logistics</p><h1 className="mt-4 max-w-3xl text-5xl font-extrabold leading-[.96] tracking-[-.06em] sm:text-6xl lg:text-7xl">Moving Australia with First-Class Service</h1><p className="mt-6 text-xl font-semibold text-gold-light">From 1 Tonne Vans to Road Trains</p><p className="mt-5 max-w-2xl leading-7 text-soft-grey">1st Class Express provides professional courier, freight, linehaul and interstate transport solutions with a strong commitment to reliability, safe handling and quality customer service.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button to="/quote">Get a Free Quote <ArrowRight size={17}/></Button><Button to="/book" variant="dark">Book a Delivery</Button><Link className="inline-flex items-center justify-center gap-2 px-3 py-3 text-sm font-bold text-white underline decoration-gold underline-offset-8 hover:text-gold" to="/services">Explore Our Services</Link></div></div><div className="mt-12 max-w-md border border-gold/40 bg-charcoal/90 p-5 shadow-2xl lg:ml-auto lg:mt-[-3rem]"><p className="text-xs font-bold uppercase tracking-[.16em] text-gold">Delivery Enquiry</p><p className="mt-3 font-display text-2xl font-bold">Need a transport solution?</p><p className="mt-2 text-sm leading-6 text-soft-grey">Tell us where the freight is moving and we’ll help identify a suitable option.</p><a className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-white hover:text-gold" href={company.phoneHref}>Speak with Div <ArrowRight size={16}/></a></div><RouteLine/><div className="mt-5 grid gap-3 text-sm font-semibold sm:grid-cols-2 lg:grid-cols-4">{['Reliable Service','Professional Drivers','Flexible Transport Solutions','Local and Interstate Coverage'].map(text=><span className="flex items-center gap-2" key={text}><CheckCircle2 size={16} className="text-gold"/>{text}</span>)}</div></div></section> }
